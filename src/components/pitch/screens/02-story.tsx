"use client";

import { BackgroundPaths } from "@/components/ui/background-paths";

export function StoryScreen() {
    return (
        <BackgroundPaths
            title="Your fleet is flying blind."
            subtitle="Cars idle in one branch while another turns customers away. Checkouts take 20 minutes. Finance finds out next week. This is how most rental operations run today."
        />
    );
}
